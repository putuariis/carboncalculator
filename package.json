{
  "name": "detektif-jejak-karbon-fullstack",
  "version": "2.0.0",
  "private": true,
  "description": "Full-stack Carbon Footprint Investigation app based on the supplied HTML prototype.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
